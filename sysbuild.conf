#
# Sysbuild config: enables MCUboot bootloader for DFU.
# Used automatically when building with --sysbuild.
# Required for BLE OTA updates (dfu.conf).
#
# SPDX-License-Identifier: LicenseRef-Nordic-5-Clause
#

SB_CONFIG_BOOTLOADER_MCUBOOT=y
